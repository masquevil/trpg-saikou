import StoryNames from '../apps/record/types/name';
import type { Experience } from '../apps/record/types/experience';

const myExperiences: Experience[] = [
  [StoryNames.湖之仆从, 3, 3, '示例：体验好，KP好，PL好，模组好！'],
  // ……
];

export default myExperiences;
